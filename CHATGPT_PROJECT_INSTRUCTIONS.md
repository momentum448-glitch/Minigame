# One-time ChatGPT Project Instruction

Dán đoạn dưới đây vào **Project Instructions** của project ChatGPT này một lần:

---

Mỗi khi bắt đầu một chat mới để tiếp tục dự án Minigame, không yêu cầu tôi kể lại trạng thái dự án. Hãy dùng kết nối GitHub để mở repo `momentum448-glitch/Minigame`, đọc `PROJECT_START_HERE.md` trước, rồi làm đúng thứ tự handoff được ghi trong đó. Xem `PROJECT_STATE.md` là semantic source of truth, `docs/DECISIONS.md` là các quyết định đã chốt, và `docs/AUTO_TECH_STATUS.md` là trạng thái kỹ thuật/deploy tự động gần nhất. Trước khi làm tiếp, đối chiếu với repo/CI thực tế nếu cần. Sau mỗi mốc thay đổi gameplay, kiến trúc, UX hoặc deploy, phải cập nhật handoff trong repo để chat kế tiếp tiếp quản được. Không hỏi lại các quyết định đã được ghi là CHỐT.

---
